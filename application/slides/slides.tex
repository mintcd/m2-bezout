 \documentclass[xcolor=dvipsnames, aspectratio=169, 10pt]{beamer}
% \usepackage{vntex}
\usetheme[sectionpage=progressbar,subsectionpage=progressbar]{metropolis}
\usepackage{tikz}
\usetikzlibrary{bayesnet}
\usetikzlibrary{arrows}
\usetikzlibrary{backgrounds}
\usetikzlibrary{overlay-beamer-styles}
\usepackage{caption}
\usepackage{animate}
\usepackage{bm}
\usepackage{mathabx}
\usepackage{graphicx}
% \setbeamertemplate{caption}[numbered]
% \usepackage{subcaption}

\usepackage[lined,commentsnumbered]{algorithm2e}
\usepackage{etoolbox}
\beamertemplatenavigationsymbolsempty
% \useinnertheme{circles}
%\useoutertheme{split}

\definecolor{gustave}{HTML}{1d2392}

\definecolor{background}{HTML}{274150}
\definecolor{sbackground}{HTML}{1d2392}
\definecolor{main-text}{HTML}{80A07C}
\definecolor{enumercolor}{HTML}{A4CCE0}
\definecolor{progress-bar}{HTML}{E6CBA0}\usecolortheme[named=sbackground]{structure} % define for item color, commented Jan 4,2024

%% TITLE PAGE 
\title[]{Realistic Mathematical Analysis
of Modern Algorithms \\ which Use SIMD Instructions for Better
Performance}
% \subtitle{Paper Presentation}

\AtBeginDocument{
	\author[Minh]{
		\begin{tabular}{lll}
      {\small Supervisors:} & \small Cyril NICAUD \\
      & Pablo ROTONDO\\\\
			{\small Applicant:} & \small CHAU Dang Minh
		\end{tabular}
	}
}
% \institute[Gustave Eiffel]{
% 	{
% 		Gustave Eiffel University \\
% 	}
% }
% TOC
\setbeamertemplate{section in toc}[circle]
\setbeamercolor{section in toc}{bg=sbackground!40, fg=black} % change color section item
 \setbeamerfont{section number projected}{size=\large}
% ENUM
  % \setbeamercolor{item projected}{bg=red!70!black,fg=white}
  \setbeamercolor{frametitle}{fg=sbackground,bg=sbackground!40} % change color bar
  \setbeamercolor{background canvas}{bg=white} % change color frame
  \setbeamercolor{normal text}{fg=black} % change text color
\setbeamercolor{progress bar}{fg=progress-bar!110,bg=progress-bar!50} % change progress bar color
    \setbeamertemplate{enumerate items}[circle]
    \setbeamerfont{itemize item projected}{size=\huge}
        \setbeamerfont{item projected}{size=\large}
% CAPTION
\setbeamertemplate{caption}{\raggedright\insertcaption\par}
% LOGO
\usepackage{textpos} 

\addtobeamertemplate{frametitle}{}{%
	\begin{textblock*}{100mm}(\textwidth,-.9cm)
		% \includegraphics[height=.7cm,width=.7cm]{logo.png}
\end{textblock*}}
\usepackage{eso-pic}
\usepackage{pgf}
% \titlegraphic{
% 	\begin{picture}(0,0)
% 		\put(420,5){\makebox(0,0)[rt]{\includegraphics[width=5cm]{logo.png}}}
% 	\end{picture}
% }
\setbeamertemplate{bibliography item}{\insertbiblabel}
\newcommand{\nologo}{\setbeamertemplate{logo}{}}
% HEADLINE
%\setbeamertemplate{headline}{}
% FOOTLINE
\setbeamercolor{custom1}{fg=black,bg=mycolor}
\setbeamercolor{custom2}{fg=black, }
\setbeamercolor{custom3}{fg=black,}
\setbeamerfont{footline}{series=\bfseries}%, family=\ttfamily}
\setbeamertemplate{footline}{%
	\leavevmode%
% 	\begin{beamercolorbox}[wd=0.25\textwidth, sep=0.4em, center]{custom1}%
% 		\strut \quad\insertshortauthor \quad (\insertshortinstitute)
% 	\end{beamercolorbox}%
% 	\begin{beamercolorbox}[wd=0.45\textwidth, sep=0.4em, center]{custom2}%
% 		\strut \insertshorttitle
% 	\end{beamercolorbox}%
	\begin{beamercolorbox}[wd=1\textwidth, sep=0.4em, right]{custom3}%
		\strut \insertframenumber/\inserttotalframenumber
	\end{beamercolorbox}%
}
% Thank you slide
\makeatletter
\let\beamer@writeslidentry@miniframeson=\beamer@writeslidentry%
\def\beamer@writeslidentry@miniframesoff{%
	\expandafter\beamer@ifempty\expandafter{\beamer@framestartpage}{}% does not happen normally
	{%else
		% removed \addtocontents commands
		\clearpage\beamer@notesactions%
	}
}
\newcommand*{\miniframeson}{\let\beamer@writeslidentry=\beamer@writeslidentry@miniframeson}
\newcommand*{\miniframesoff}{\let\beamer@writeslidentry=\beamer@writeslidentry@miniframesoff}
\makeatother

\input{commands.tex}

\usepackage[most]{tcolorbox}
\tcbuselibrary{breakable,skins}

% Shared counter for all theorem-like environments
\newcounter{mytheoremcounter}

\newtcolorbox{mydefinition}[1][]{%
  enhanced,
  title={Definition \stepcounter{mytheoremcounter}\themytheoremcounter\ifstrempty{#1}{}{ (#1)}},
  % attach boxed title to top left={yshifttext=-1mm},
  colback=white,
  colframe=gustave!50!black,
  fonttitle=\bfseries,
  boxed title style={
    size=small,
    colback=gustave!50!black,
    colframe=gustave!50!black,
  },
}

\newtcolorbox{mytheorem}[1][]{%
  enhanced,
  title={Theorem \stepcounter{mytheoremcounter}\themytheoremcounter\ifstrempty{#1}{}{ (#1)}},
  % attach boxed title to top left={yshifttext=-1mm},
  colback=white,
  colframe=gustave!50!black,
  fonttitle=\bfseries,
  boxed title style={
    size=small,
    colback=gustave!50!black,
    colframe=gustave!50!black,
  },
}

\newtcolorbox{myproposition}[1][]{%
  enhanced,
  title={Proposition \stepcounter{mytheoremcounter}\themytheoremcounter\ifstrempty{#1}{}{ (#1)}},
  % attach boxed title to top left={yshifttext=-1mm},
  colback=white,
  colframe=gustave!50!black,
  fonttitle=\bfseries,
  boxed title style={
    size=small,
    colback=gustave!50!black,
    colframe=gustave!50!black,
  },
}


% DOCUMENT
\begin{document}
\date{}
\begin{frame}[noframenumbering,plain]
  \titlepage
\end{frame}

\begin{frame}{Education: Maths and CS}

  \textbf{MSc in Mathematics \& Computer Science} \hfill \textit{Gustave Eiffel Univ., in progress}
  \begin{itemize}
    % \item Labex Bézout Scholar, ranked 1st/12 (excluding internship)
    \item Relevant coursework: combinatorics, discrete optimization and complexity theory.
  \end{itemize}

  \textbf{MSc in Applied Mathematics} \hfill \textit{Univ. of Limoges, 2025}
  \begin{itemize}
    % \item Ranked 1st/10
    \item Relevant coursework: stochastic optimization.
  \end{itemize}

  \textbf{BEng in Computer Science} \hfill \textit{Univ. of Technology, Vietnam, 2024}
  \begin{itemize}
    \item Relevant coursework: data structures and algorithms, and compilers.
  \end{itemize}

  % Sparked interest in logic, computation and AI.
\end{frame}

\begin{frame}{Research Experience: Analysis of Algorithms \& Formal Methods}
  \begin{columns}[t]
    \begin{column}{0.72\textwidth}
      \textbf{Compatibility between Open Automata and Session Types}
    \end{column}
    \begin{column}{0.28\textwidth}
      \raggedleft\textit{M2 internship, \\ Telecom Paris, 2025}
    \end{column}
  \end{columns}
  \vspace{-0.5em}
  \begin{itemize}
    \item \textbf{Core Result:} Proved that a simplified OA model is equivalent to multiparty session types, mapping safe composition to subtyping.
    \item \textbf{Takeaway for PhD:} Foundational experience in models of concurrent computing, useful for building models of SIMD parallelism.
  \end{itemize}

  \vspace{0.8em}
  \begin{columns}[t]
    \begin{column}{0.72\textwidth}
      \textbf{Complexity Analysis of the Regex Matcher Hyperscan}
    \end{column}
    \begin{column}{0.28\textwidth}
      \raggedleft\textit{M2 Internship, \\ LIGM, in progress}
    \end{column}
  \end{columns}

  \begin{itemize}
    \item \textbf{Core Analyses:} FDR string matcher (SIMD extension of Shift-Or), correctness of decomposition-based matching, and longest literal in average.
    \item \textbf{Takeaway for PhD:} Techniques for complexity analysis (from analytic combinatorics and probability theory), and experience with SIMD algorithms and their implementation.
  \end{itemize}
\end{frame}

\begin{frame}{Research Experience: Analysis of Algorithms - Details}
  Example of a regular expression (regex): $(a+b\cdot c)\cdot d\cdot e^*$, equivalent to $(a \cdot d+b \cdot c \cdot d)e^*$.

  Hyperscan extracts literals ($a\cdot d$ and $b\cdot c\cdot d$ in the example) from a regex, then
  \begin{enumerate}
    \item matches all literals using FDR (or another string matcher \footnote{Qiu et al. (2021). Teddy: An efficient SIMD-based literal matching engine for scalable deep packet inspection.}) \textit{at once},
    \item verifies subregex matches using the previous results.
  \end{enumerate}

  We assume a distribution over the regex trees, currently the BST-like \footnote{Binary Search Tree}.

  \only<1-7>{Useful statistics to analyze asymptotically: the average of the number of literals, and the average longest length of the literals (on which we are concentrating).

    The second statistic leads to analyzing the longest literal $Y_n$ that a subtree \textit{yields} to its parent.}

  \only<2-6>{
    \begin{tikzpicture}[remember picture,overlay]
      % anchor point on the page
      \coordinate (treecenter) at ([xshift=-2cm,yshift=-2.5cm]current page.north east);

      % slide 2: big cloud
      \node[draw,cloud,minimum size=2cm,visible on=<2>] (rootcloud) at ([yshift=-1.25cm]treecenter) {6 nodes};

      % root symbol '+' appears from slide 3 and stays
      \node[visible on=<3->] (plus) at (treecenter) {\textcolor{gustave}{$+$}};

      % small clouds for slide 3 (placed at final positions but only visible on slide 3)
      \node[draw,cloud,visible on=<3>] (a_cloud)    at ([xshift=-0.75cm,yshift=-0.75cm]treecenter) {1};
      \node[draw,cloud,visible on=<3>] (star_cloud) at ([xshift= 0.75cm,yshift=-0.75cm]treecenter) {4};
      \draw<3> (plus) -- (a_cloud);
      \draw<3> (plus) -- (star_cloud);

      % slide 4: show text 'a' and '*' and a small cloud '3' (placed at final coordinates)
      \node[visible on=<4->] (a_lbl)    at ([xshift=-0.75cm,yshift=-0.75cm]treecenter) {\textcolor{gustave}{$a$}};
      \node[visible on=<4->] (star_sym)  at ([xshift= 0.75cm,yshift=-0.75cm]treecenter) {\textcolor{gustave}{$\ast$}};
      \node[draw,cloud,visible on=<4>] (dot_cloud) at ([xshift=0.75cm,yshift=-1.5cm]treecenter) {3};
      \draw<4-> (plus) -- (a_lbl);
      \draw<4-> (plus) -- (star_sym);
      \draw<4> (star_sym) -- (dot_cloud);

      % slide 5: show dot symbol and two child clouds (1,1)
      \node[visible on=<5->] (dot_sym) at ([xshift=0.75cm,yshift=-1.5cm]treecenter) {\textcolor{gustave}{$\cdot$}};
      \node[draw,cloud,visible on=<5>] (b_cloud) at ([xshift=0cm,yshift=-2.25cm]treecenter) {1};
      \node[draw,cloud,visible on=<5>] (c_cloud) at ([xshift=1.5cm,yshift=-2.25cm]treecenter) {1};
      \draw<5> (star_sym) -- (dot_sym);
      \draw<5> (dot_sym) -- (b_cloud);
      \draw<5> (dot_sym) -- (c_cloud);

      % slide 6: replace clouds with final labels 'b' and 'c' (same coordinates)
      \node[visible on=<6->] (b_lbl) at ([xshift=0cm,yshift=-2.25cm]treecenter) {\textcolor{gustave}{$b$}};
      \node[visible on=<6->] (c_lbl) at ([xshift=1.5cm,yshift=-2.25cm]treecenter) {\textcolor{gustave}{$c$}};
      \draw<6> (star_sym) -- (dot_sym);
      \draw<6> (dot_sym) -- (b_lbl);
      \draw<6> (dot_sym) -- (c_lbl);
    \end{tikzpicture}
  }
\end{frame}

\begin{frame}{Research Experience: Analysis of Algorithms - Details}
  \vspace{-1.75em}
  $$Y_{n+2} = \underbrace{I^{(n)}}_{\substack{\text{indicator of root} \\ \text{being concat.}}}\underbrace{\left(Y_{U_n} + Y_{n+1-U_n} + 1\right)}_{\substack{\text{take all of the subtrees,}\\ \text{plus the current}}} + \underbrace{J^{(n)}}_{\substack{\text{indicator of root} \\ \text{being alternation}}} \underbrace{\max\left(Y_{U_n}, Y_{n+1-U_n}\right)}_{\substack{\text{take the longer}\\\text{of the subtrees}}}, \quad Y_1=Y_2=0.$$
  For $\mathbb{E}[Y_n]$ when $\mathbb{E}[I^{(n)}]+\mathbb{E}[J^{(n)}] := u+v>1/2$ (agreeing with real data),
  \vspace{-0.5em}
  \begin{center}
    \makebox[\textwidth][r]{%
      \begin{minipage}{0.94\textwidth}
        \begin{columns}[c]
          \begin{column}{0.72\textwidth}
            \textbf{Lower bound} $\Omega(n^\alpha)$ where $\alpha$ solves $\alpha + v2^{-\alpha}=2u+v-1$
          \end{column}
          \begin{column}{0.02\textwidth}
            \centering
            \rule{0.8pt}{1.5em}
          \end{column}
          \begin{column}{0.36\textwidth}
            \centering
            \textbf{Upper bound} $O(n^{2(u+v)-1})$
          \end{column}
        \end{columns}
      \end{minipage}%
    }
  \end{center}
  \vspace{-0.5em}
  \textbf{Conjecture:} in some domain of $(u,v)$, $\mathbb{E}[Y_n]\sim Cn^{\alpha^\star}$.

  \begin{columns}[c]
    \begin{column}{0.39\textwidth}
      \centering
      \includegraphics[width=\linewidth]{img/05-03-100000-200000.png}
    \end{column}
    \begin{column}{0.39\textwidth}
      \centering
      \includegraphics[width=\linewidth]{img/04-015-100000-200000.png}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{PhD Topic}
  \textbf{Context and Motivation:}
  \begin{itemize}
    \item Developers optimize their library algorithms in a fine-grained way e.g., they make assumptions on the input or leverage hardware features such as SIMD instructions.
    \item Researchers are interested in explaining if such optimizations are indeed efficient. Examples include both confirmation (TimSort \footnote{Auger, N., Jugé, V., Nicaud, C., \& Pivoteau, C. (2018). On the worst-case complexity of TimSort.}) and rejection (Lua's hybrid table \footnote{Martínez, C., Nicaud, C., \& Rotondo, P. (2022). A probabilistic model revealing shortcomings in Lua’s hybrid tables.})
  \end{itemize}

  \textbf{Goal:} Develop a framework for analyzing algorithms that use SIMD.

  \textbf{Fruitfulness:}
  \begin{itemize}
    \item Direct applications to algorithm design.
    \item Requirements naturally lead to deep mathematical questions, e.g., $\mathbb{E}[Y_n]$ may not be analyzable by classical techniques.
  \end{itemize}
\end{frame}

% \begin{frame}{Outline}
%   \tableofcontents
% \end{frame}

% \input{sections/introduction.tex}
% \input{sections/risk-bounds}
% \input{sections/estimating-rademacher}
% \input{sections/example}
% \input{sections/experiment}


\begin{frame}[noframenumbering,plain]
  \begin{center}
    \Huge\textbf{Thank you for attention !}
  \end{center}
\end{frame}

\input{sections/appendix.tex}
% \printbsibliography
\end{document}