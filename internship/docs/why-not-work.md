1. In the sum case, we need the Zolotarev metric for useful upper bounds. But the Zolotarev metric requires the mean to be fixed. Fortunately, by linearity, we can center.
2. In the max case, the Wasserstein metric is enough.
3. When combining, we cannot center. So we have to somehow keep the same mean.